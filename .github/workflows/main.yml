name: "primera automatizacion"

on: [workflow_dispatch] #disparador manual

env:
  Nombre: "Eliany"

jobs:
  job1:
    runs-on: ubuntu-latest
    
    steps:
      - name: concatenar
    
        run: |
         apellido="Gaitan" 
         echo ${{env.Nombre}} 
         echo $apellido
